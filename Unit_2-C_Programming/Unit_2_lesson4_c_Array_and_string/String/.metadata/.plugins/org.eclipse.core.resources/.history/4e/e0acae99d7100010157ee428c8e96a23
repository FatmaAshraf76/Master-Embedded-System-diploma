#include <stdio.h>

int main(void) {
    char str[100];
    char rev[100];
    int  i;

    printf("Enter a string: ");
    fflush(stdout);
    gets(str);
    printf("the orignal text: %s ",str);
    int size=strlen(str);
    for (i = 0; str[i] != '\0'; i++)
    {
        rev[size-i-1]=str[i];
    }
    printf("the reversed text: %s ",rev);
    return 0;
}
